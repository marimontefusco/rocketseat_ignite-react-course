import { render } from 'react-dom';
import { App } from './App';

render(<App />, document.getElementById('root'))

//render(param1, param2)
//param1 -> o que eu quero renderizar, o q quqero exibir em tela?
//param2 -> dentro de qual elemento quero renderizar essa informação? lá na div id='root' do meu documento html
